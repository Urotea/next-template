schema: "graphql/v1/schema.graphql"
documents:
  - "graphql/v1/queries/**/*.graphql"
generates:
  ./src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
      - typescript-operations
      - typescript-graphql-request
    config:
      avoidOptionals: true # defualt false
      preResolveTypes: true # default true
