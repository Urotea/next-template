query GetUser($id: String!) {
  user(id: $id) {
    user {
      id
      name
      # age
      todos {
        id
        # owner
        title
        detail
        state
      }
      # memberOf
    }
  }
}
