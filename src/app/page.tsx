import CustomButton from "@/components/server/CustomButton";

const Page = () => {
  return (
    <>
      <CustomButton type="primary" size="sm">
        Button
      </CustomButton>
    </>
  );
};

export default Page;
