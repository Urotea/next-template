import type { NextPage } from "next";
import { Box, TextField, Typography } from "@mui/material";

const Home: NextPage = () => {
  return <Box textAlign="center" mt="10px"></Box>;
};

export default Home;
